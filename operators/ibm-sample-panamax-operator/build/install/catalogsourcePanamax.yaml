apiVersion: operators.coreos.com/v1alpha1
kind: CatalogSource
metadata:
  name: ibm-sample-panamax-catalog
  namespace: openshift-marketplace
spec:
  displayName: ibm-sample-panamax-catalog 
  publisher: IBM Content
  sourceType: grpc
  # TODO: change to use tag of latest for catalog ONLY
  image: icr.io/cpopen/ibm-sample-panamax-catalog:latest
  updateStrategy:
    registryPoll:
      interval: 45m
